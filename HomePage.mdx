---
title: "HomePage"
description: "Landing page of our application"
---

```



import Header from "../header.jsx";
import Footer from "@/components/footer.tsx";

import Section2 from "./section2/section2.jsx";
import React from "react";
import Section3 from "./section3/section3.jsx";
import Section4 from "./section4/section4.jsx";
import homepageData from "./homePage.json";
import useFetchJsonData from "@/hooks/useFetchJsonData";
import { store } from "@/store/store.js";
import { Provider } from "react-redux";
import homepageData from "@/components/homePage/homepage.json";
import SeventhSection from "./seventhSection/seventhSection.jsx";
import analytics from "../../utils/analytics.jsx";

import LoginPopup from "../LoginPopup/index.jsx";
import NinthSection from "./ninthSection/ninthSection.jsx";
import EigthSection from "./eightSection/eigthSection.jsx";
import SelectGradePopup from "../common/SelectGradePopup.jsx";
import BookSessionPopup from "../bookSessionPopup/index.jsx";
import Section1 from "./section1/section1.jsx";
import Section5 from "./section5/section5.jsx";
import Section6 from "./section6/section6.jsx";
export async function getServerSideProps(context) {
  try {
    const res = await fetch(
      "https://s3.ap-south-1.amazonaws.com/ilwebsite2.devinfinitylearn.in/Homepage/JSON_File/homepage.json"
    );
    if (!res.ok) {
      throw new Error(`Failed to fetch data, status code: ${res.status}`);
    }
    const homepageData = await res.json();
    console.info(homepageData);
    return { props: { homepageData } };
  } catch (error) {
    return { props: { homepageData: null, error: error.message } };
  }
}
async function getData() {
  const res = await fetch(
    "https://s3.ap-south-1.amazonaws.com/ilwebsite2.devinfinitylearn.in/Homepage/JSON_File/homePage.json"
  );
  if (!res.ok) {
    // This will activate the closest `error.js` Error Boundary
    throw new Error("Failed to fetch data");
  }
  return res.json();
}

export default async function HomePage({homepage, classFor, examFor }) {

  return (
    // <Provider store={store}>
    <div className=" poppins w-screen">
      {/* <Header /> */}
    console.log(homepage,"dataa")
      <Section1 homepageDat={homepageData} />
      <Section2 homepageDat={homepageData} />
      <Section3 homepageDat={homepageData} />
      <Section4
        homepageDat={homepageData}
        classFor={classFor}
        examFor={examFor}
      />
      <Section5 homepageDat={homepageData} />
      <Section6 homepageDat={homepageData} />
      <SeventhSection
        homepageDat={homepageData}
        // classFor={params.class}
        // examFor={params.exam}
      />
      <EigthSection homepageDat={homepageData} />
      <NinthSection homepageDat={homepageData} />
      {/* <Footer /> */}
      {/* <LoginPopup />
      <SelectGradePopup /> */}
      {/* <PackageSubscrption /> */}
      {/* <BookSessionPopup /> */}
    </div>
    //  </Provider>
  );
}
export async function getServerSideProps(context) {
  try {
    const response = await fetch("https://s3.ap-south-1.amazonaws.com/ilwebsite2.devinfinitylearn.in/Homepage/homePage.json");
    if (!response.ok) {
      throw new Error(`Failed to fetch data, status code: ${response.status}`);
    }
    const homepageData = await response.json();
    return { props: { homepageData } };
  } catch (error) {
    console.error("Error fetching data:", error);
    return { props: { homepage: null, error: error.message } };
  }
}

```

The above code defines a React component named HomePage that fetches data from a JSON file and
renders different sections based on the fetched data.
@returns The code snippet is exporting an asynchronous function named `HomePage` which takes in
parameters `homepage`, `classFor`, and `examFor`. Within the function, it renders various sections
of a homepage using the provided `homepageData` and other props. Additionally, there are two
`getServerSideProps` functions defined to fetch data from an external API and return it as props for
server-side rendering.
