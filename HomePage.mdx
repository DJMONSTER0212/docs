---
title: 'HomePage'
description: 'Landing page of our application'
---

The above code defines a React component named HomePage that fetches data from a JSON file and
 renders different sections based on the fetched data.
 @returns The code snippet is exporting an asynchronous function named `HomePage` which takes in
 parameters `homepage`, `classFor`, and `examFor`. Within the function, it renders various sections
 of a homepage using the provided `homepageData` and other props. Additionally, there are two
`getServerSideProps` functions defined to fetch data from an external API and return it as props for
 server-side rendering.